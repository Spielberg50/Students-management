<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gestiondesetudiants">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/emp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/emp_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AffichageActivity"
            android:screenOrientation="landscape"></activity>
        <activity android:name=".EmploieActivity" />
        <activity
            android:name=".MoyenneActivity"
            android:screenOrientation="landscape" />
        <activity android:name=".CalculeActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>